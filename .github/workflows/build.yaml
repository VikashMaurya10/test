name: CI - Lint, Build, and Env Check

on:
  pull_request:
    branches:
      - master

jobs:
  lint-build-check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    env:
      API_URL: ${{ secrets.API_URL }}
      NEXT_PUBLIC_APP_URL: ${{ secrets.NEXT_PUBLIC_APP_URL }}
      NEXTAUTH_SECRET: ${{ secrets.NEXT_AUTH_SECRET }}
      NEXTAUTH_URL: ${{ secrets.NEXT_AUTH_APP_URL }}
      REVIEWDOG_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1

      - name: Install Dependencies
        run: bun install

      - name: Install ESLint and Reviewdog (dev deps)
        run: |
          bun add -d eslint
          curl -sfL https://raw.githubusercontent.com/reviewdog/reviewdog/master/install.sh | sh -s -- -b . latest

      - name: Run ESLint via Reviewdog
        continue-on-error: true
        run: |
          bunx eslint . -f json | tee eslint_output.json | ./reviewdog -f=eslint -name="eslint" -reporter=github-pr-review

      - name: Check Environment Variables
        run: |
          echo "Checking required ENV variables..."
          test -n "$API_URL" && echo "API_URL loaded"
          test -n "$NEXT_PUBLIC_APP_URL" && echo "NEXT_PUBLIC_APP_URL loaded"
          test -n "$NEXTAUTH_SECRET" && echo "NEXTAUTH_SECRET loaded"
          test -n "$NEXTAUTH_URL" && echo "NEXTAUTH_URL loaded"

      - name: Build Project
        id: buildstep
        run: |
          bun run build 2>&1 | tee build.log
        continue-on-error: true

      - name: Report Build Failure in Summary
        if: steps.buildstep.outcome == 'failure'
        run: |
          echo "### ❌ Build Failed" >> $GITHUB_STEP_SUMMARY
          echo '```log' >> $GITHUB_STEP_SUMMARY
          sed -r "s/\x1B\[[0-9;]*[mK]//g" build.log | grep -E "Error|Syntax" | tail -n 30 >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

      - name: Prepare Clean Build Log for Comment
        if: steps.buildstep.outcome == 'failure'
        run: |
          sed -r "s/\x1B\[[0-9;]*[mK]//g" build.log | grep -E "Error|Syntax" | tail -n 30 > short_build.log

      - name: Comment Build Failure on PR
        if: steps.buildstep.outcome == 'failure'
        uses: actions/github-script@v6
        with:
          script: |
            const fs = require('fs');
            const log = fs.readFileSync('short_build.log', 'utf8');
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `❌ **Build failed.**\n\n\`\`\`log\n${log.trim()}\n\`\`\``
            });

      - name: Comment Setup Success on PR
        if: ${{ success() }}
        uses: actions/github-script@v6
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: "✅ All setup completed. Ready to deploy on server."
            })

      - name: Fail Workflow if Build Fails
        if: steps.buildstep.outcome == 'failure'
        run: exit 1
      
